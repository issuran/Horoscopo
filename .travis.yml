os: osx
osx_image: xcode10.3
language: objective-c
script:
  - set -o pipefail && xcodebuild -workspace "Horoscopo.xcworkspace" -scheme "Horoscopo" -destination "platform=iOS Simulator,OS=13.3,name=iPhone 11" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO | xcpretty

branches:
 only:
 - master
